== README

Simple Address book that allows a user to view, add, edit and delete contacts
from the database.

The default rails location/page routes to /contacts#index as a 'landing page'.


=== USES:

Ruby: ruby 2.2.2p95

Rails: 4.2.1

Database: default SQlite
